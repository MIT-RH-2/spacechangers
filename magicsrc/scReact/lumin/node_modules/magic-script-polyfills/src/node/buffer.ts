import { assert, utf8Encode } from "../utils2.js";

export class Buffer extends Uint8Array {
    public static from(array: ArrayLike<number>): Buffer;
    public static from(buffer: Buffer): Buffer;
    public static from(str: string, encoding?: string): Buffer;
    public static from(
        init: ArrayLike<number> | ArrayBuffer | Buffer | string,
        encodingOrOffset?: string | number, maybeLength?: number): Buffer {
        if (typeof init === "string") {

        }
    }

    public static alloc(size: number): Buffer {
        return new Buffer(size);
    }

}
